// Exercise 022: Multiple Variable Declaration
// Learning objective: Learn to declare multiple variables at once using tuple destructuring

fn main() {
    // TODO: Declare multiple variables in one line using a tuple
    // let (name, age, score) = ("Alice", 25, 95);
    // Then print all three values
    
    // let (name, age, score) = (// ...
    // println!("Name: {}, Age: {}, Score: {}", name, age, score);
    
    // TODO: Declare two variables using tuple destructuring
    // for the width and height of a rectangle
    // let (width, height) = // ...
    // let area = width * height;
    // println!("Rectangle: {} x {} = {} square units", width, height, area);
    
    // TODO: Swap two variables using tuple destructuring
    let mut a = 5;
    let mut b = 10;
    println!("Before swap: a = {}, b = {}", a, b);
    
    // Swap using tuple destructuring
    // (a, b) = (// ...
    // println!("After swap: a = {}, b = {}", a, b);
    
    // TODO: Use _ to ignore values you don't need
    // let (first, _, third) = (1, 2, 3);
    // println!("First: {}, Third: {}", first, third);
}
