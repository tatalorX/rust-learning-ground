// Exercise 007: Comparison Operators
// Learning objective: Learn how to compare values using comparison operators

fn main() {
    let x = 10;
    let y = 20;
    
    // TODO: Check if x is equal to y and print the result
    let is_equal = // ...
    println!("{} == {} ? {}", x, y, is_equal);
    
    // TODO: Check if x is NOT equal to y
    let is_not_equal = // ...
    println!("{} != {} ? {}", x, y, is_not_equal);
    
    // TODO: Check if x is less than y
    let is_less = // ...
    println!("{} < {} ? {}", x, y, is_less);
    
    // TODO: Check if x is greater than y
    let is_greater = // ...
    println!("{} > {} ? {}", x, y, is_greater);
    
    // TODO: Check if x is less than or equal to 10
    let is_less_or_equal = // ...
    println!("{} <= 10 ? {}", x, is_less_or_equal);
    
    // TODO: Check if y is greater than or equal to 20
    let is_greater_or_equal = // ...
    println!("{} >= 20 ? {}", y, is_greater_or_equal);
}
