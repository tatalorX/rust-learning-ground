// Exercise 008: Boolean Logic
// Learning objective: Learn to use logical operators: && (AND), || (OR), ! (NOT)

fn main() {
    let sunny = true;
    let warm = true;
    let weekend = false;
    
    // TODO: Check if it's sunny AND warm (both must be true)
    let beach_weather = // ...
    println!("Good beach weather? {}", beach_weather);
    
    // TODO: Check if it's sunny OR warm (at least one must be true)
    let nice_day = // ...
    println!("Nice day? {}", nice_day);
    
    // TODO: Check if it's the weekend OR warm (use ||)
    let can_relax = // ...
    println!("Can relax? {}", can_relax);
    
    // TODO: Check if it's NOT the weekend
    let is_weekday = // ...
    println!("Is it a weekday? {}", is_weekday);
    
    // TODO: Check if it's sunny AND warm AND NOT weekend
    let perfect_workday = // ...
    println!("Perfect workday weather? {}", perfect_workday);
}
