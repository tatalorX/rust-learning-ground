// Exercise 018: Functions - Multiple Returns (Tuples)
// Learning objective: Learn to return multiple values using tuples

// TODO: Create a function named 'divide_with_remainder' that takes two i32 parameters: dividend and divisor
// It should return a tuple containing the quotient and remainder
// Hint: (a / b, a % b)
// fn divide_with_remainder(a: i32, b: i32) -> (i32, i32) {
//     // TODO: Return a tuple with quotient and remainder
// }

// TODO: Create a function named 'min_max' that takes two i32 parameters
// It should return a tuple containing (smaller, larger)
// fn min_max(a: i32, b: i32) -> (i32, i32) {
//     // TODO: Return (smaller, larger)
// }

fn main() {
    // TODO: Call divide_with_remainder with 17 and 5
    // Destructure the result into quotient and remainder
    // let (quotient, remainder) = divide_with_remainder(17, 5);
    // println!("17 divided by 5 is {} with remainder {}", quotient, remainder);
    
    // TODO: Call min_max with 42 and 17
    // Print both values from the returned tuple
    // let result = min_max(42, 17);
    // println!("Smaller: {}, Larger: {}", result.0, result.1);
    
    // TODO: Call min_max with 10 and 100, using destructuring
    // let (min, max) = min_max(10, 100);
    // println!("Between 10 and 100: min={}, max={}", min, max);
}
